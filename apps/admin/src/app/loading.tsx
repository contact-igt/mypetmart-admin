export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-cream-bg p-6">
      <p className="text-sm font-medium text-text-primary/60">Loading admin…</p>
    </main>
  );
}